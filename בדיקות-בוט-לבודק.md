# 🧪 בדיקות בוט Kids & Fun — מדריך לבודק

> **לבודק:** אתה מדמה הורה אמיתי שכותב לבוט ב-WhatsApp.
> אין כפתורים בוואטסאפ — פשוט **כותבים** בעברית רגילה כמו שהורה היה כותב ("כן", "אני רוצה לבטל").

---

## לפני שמתחילים — חשוב

1. נכנסים לדשבורד → לשונית **"סימולטור"**.
2. **לפני כל תסריט** — לוחצים **"התחל שיחה חדשה"** (חובה! אחרת השיחה הקודמת ממשיכה).
3. בשדה **"טלפון לבדיקה"** — מזינים **0542662263** (ההורה "רפאל טייב" — הבוט מזהה אותו אוטומטית).
   באיזה תסריט צריך טלפון אחר / ריק — כתוב במפורש.
4. כותבים בעברית טבעית. אפשר לכתוב מילה ("הוראת קבע") או מספר ("2") — שניהם עובדים.

### איפה בודקים את התוצאה (לשוניות הדשבורד)
- **פניות** — כאן נפתחות כל הפניות לטיפול הצוות (רישום, כשל תשלום, ביטול וכו'). *(לא "משימות" — קוראים לזה "פניות".)*
- **רישומים** — רישומי צהרון/קייטנה והסטטוס שלהם.
- **הורים** — כרטיסי ההורים והילדים.

---

## 🟢 תסריט 1 — רישום חדש לצהרון
**הכנה:** לנקות את שדה "טלפון לבדיקה" (הורה חדש) → "התחל שיחה חדשה".

כותבים אחד אחרי השני (ממתינים לתשובת הבוט בכל שלב):
1. `אני רוצה לרשום את הבת שלי לצהרון`
2. `שרון`
3. `נועה כהן`
4. `א`

**צפוי:** הבוט בודק קיבולת ושולח **קישור לטופס הרשמה**.
- בלשונית **פניות** — נפתחת פנייה חדשה (סוג: רישום).
- בלשונית **רישומים** — הרישום של נועה יופיע **רק אחרי מילוי הטופס** (לחיצה על הקישור + שליחה). זה לא מיידי מהצ'אט.

---

## 🟢 תסריט 2 — הורה קיים שואל על ילד שלו
**הכנה:** טלפון לבדיקה = `0546164546` → "התחל שיחה חדשה".
1. `אני רוצה לרשום ילד`

**צפוי:** הבוט **מזהה אותך**, לא מתחיל מאזור — מציג מה כבר רשום ושואל מה לעשות עכשיו.

---

## 🟣 תסריט 3 — ביטול הרשמה לצהרון
1. `אני רוצה לבטל את הצהרון`
2. `דני בדיקה` (שם הילד)
3. `כן` (אישור)

**צפוי:** הסבר על תקנון ה-15 בחודש + "בקשת הביטול נקלטה".
- **רישומים** — דני בדיקה עובר לסטטוס "בוטל".
- **פניות** — נפתחת פנייה לנציגה.

**בדיקת חוכמה (כל אחד בשיחה חדשה):** כל הניסוחים האלה צריכים להוביל לאותה זרימת ביטול — לא לרישום ולא לנציגה:
- `לבטל צהרון`
- `לא רוצה להמשיך בצהרון`
- `לעזוב את הצהרון`
- `להפסיק את הצהרון`

---

## 🟢 תסריט 4 — רישום לקייטנה
1. `אני רוצה לרשום לקייטנה`
2. `לרשום ילד חדש`

**צפוי:** הבוט שולח **קישור לחנות באתר** (kidsandfun.co.il/shop).

---

## 🟢 תסריט 5 — בדיקה אם ילד נרשם לקייטנה
1. `אני רוצה לבדוק אם הילד שלי נרשם לקייטנה`
2. `לבדוק אם כבר נרשמתי`
3. `שי וינטראוב`
4. `2339` (4 ספרות אחרונות של ת"ז)

**צפוי:** זיהוי מיידי + תשובה כן/לא + פרטי הקייטנה (בלי להמתין לנציגה).

---

## 🟢 תסריט 6 — שאלות לו"ז וחגים
כל שאלה בשיחה נפרדת ("התחל שיחה חדשה"):
- `מתי שעות הצהרון?`
- `כמה ימים בשבוע אפשר?`
- `אפשר רק יומיים?`

**צפוי:** תשובה מיידית (לא "נציגה תחזור").

---

## 🟢 תסריט 7 — איסוף מוקדם
1. `אני רוצה לאסוף את הילד מוקדם`
2. `נועה כהן`
3. `15:00`
4. `סבתא רחל`

**צפוי:** "בקשת האיסוף התקבלה" + פנייה חדשה בלשונית **פניות** (סוג: איסוף מוקדם).

---

## 🟢 תסריט 8 — סטטוס תשלום
1. `מה הסטטוס של התשלום שלי`
2. `סטטוס התשלום שלי`
3. `ליאו עצמון` (שם ילד אמיתי במערכת)

**צפוי:** 🟢/🟡/🔴 + סכום + מספר תשלום + תאריך.

---

## 🔵 תסריט 9 — כשל תשלום מצד ההורה (5 ענפים)
**עיקרון:** הכל אוטומטי. נציגה רק בענף 5 ("אחר"), כשבאמת אין דרך לחזות.
**הכנה לכל ענף:** טלפון `0542662263` → "התחל שיחה חדשה".

### 9.1 — החלפתי כרטיס
1. `התשלום נכשל`
2. `החלפתי כרטיס`
3. `כן` (אישור שאתה ההורה של רפאל טייב)

**צפוי:** קישור PayPlus שמתחיל ב-`paymentsdev.payplus.co.il`. בלשונית **פניות** — פנייה "החלפת כרטיס — רפאל טייב" בעדיפות גבוהה.

### 9.2 — אמצעי תשלום אחר
1. `התשלום נכשל`
2. `אני רוצה אמצעי תשלום אחר`
3. `כן` (זיהוי)
4. בוחרים אמצעי (כותבים אחד מהם):
   - `הוראת קבע` או `אשראי` → קישור PayPlus דינמי
   - `מזומן` → הוראות בכתב + פנייה
   - `צ׳קים` → הוראות בכתב + פנייה
   - `העברה בנקאית` → פרטי חשבון + פנייה

**צפוי:** בכל מקרה נפתחת **פנייה** בלשונית פניות.

### 9.3 — שינוי תאריך חיוב
1. `התשלום נכשל`
2. `אני רוצה לשנות את תאריך החיוב`
3. `כן`
4. `10` (יום בחודש)

**צפוי:** הבוט מאשר את העדכון. נפתחת **פנייה** בלשונית פניות.

### 9.4 — תחזרו אליי בעוד כמה ימים
1. `התשלום נכשל`
2. `תחזרו אלי בעוד כמה ימים`
3. `כן`
4. `עוד 5 ימים` (או "בעוד שבוע", או "ב-25 לחודש")

**צפוי:** "נחזור אליך ב-[תאריך]". נפתחת **פנייה** בלשונית פניות (התזכורת מופיעה כפנייה — אין לשונית "תזכורות" נפרדת).

### 9.5 — אחר
1. `התשלום נכשל`
2. `אחר`
3. `המצב מסובך, תתקשרו אלי בבקשה`

**צפוי:** "תודה שפירטת, נציגה תחזור" + **פנייה דחופה** בלשונית פניות.

### בדיקת זיהוי כפול (בלי טלפון)
מנקים את "טלפון לבדיקה" → "התחל שיחה חדשה" → מריצים 9.1. הבוט יבקש שם ילד מלא:
- `רפאל` (שם אחד) → צריך לדחות ולבקש שם פרטי + משפחה
- `רפאל 1` (מספר בשם) → צריך לדחות
- `רפאל טייב` → צריך לקבל ולהמשיך לקישור

---

## 🟢 תסריט 10 — הסדרת תשלום (קישור דינמי)
**הכנה:** לנקות את "טלפון לבדיקה" → "התחל שיחה חדשה".
1. `אני רוצה להסדיר תשלום`
2. `להסדיר` (מתוך התפריט שמופיע)
3. `הוראת קבע`
4. `נועה כהן`
5. `כרמל`

**צפוי:** קישור PayPlus דינמי להוראת קבע חודשית. סכום: 880₪ (כרמל) / 920₪ (שרון) / 950₪ (תל אביב). בסנדבוקס יוצג 5₪.

---

## 💳 בדיקת תשלום בכרטיס (סנדבוקס — אין חיוב אמיתי!)
אחרי שהבוט נותן קישור (9.1 / 9.2-קבע / 10) — לוחצים עליו וממלאים:

| שדה | ערך (להזין **בדיוק** כך — לא הפוך!) |
|------|------|
| מספר כרטיס | `5326 1402 8077 9844` |
| תוקף | `09/26` |
| CVV | `000` |
| ת"ז | `000000018` |
| שם בעל הכרטיס | לפי התסריט (רפאל טייב / נועה כהן) |

- בתחתית צריך להופיע **5.00₪** (בסנדבוקס). אם מופיע 799/880 — לעצור ולדווח.
- אחרי "ביצוע התשלום" → דף **"התשלום התקבל בהצלחה 🎉"** (לא 404).
- בלשונית **הורים / רישומים** — הנתונים מתעדכנים אחרי התשלום.

### ⚠️ מתי לעצור ולדווח
- שגיאה 132 / 125 חוזרת.
- הסכום מופיע 799/880 ₪ במקום 5 ₪.
- הקישור מתחיל ב-`payments.payplus` (בלי `dev`) — סימן שרץ מול פרודקשן.
- הבוט מציג "תקלה זמנית".

**מצב נוכחי:** המערכת רצה ב-Sandbox של PayPlus. כל פעולה בטוחה — אין חיוב אמיתי ולקוחות אמיתיים לא מושפעים.
