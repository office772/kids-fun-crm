# הנדהוף לסקירה חוזרת — חלק ב' #7, ביטול עמום ואמצעי-תשלום, commit `61c4d5a`

astra: שלוש הדוגמאות הקודמות תוקנו. נותר: "רוצה לבטל"/"לבטל" (בלי מטרה) עדיין התחילו ביטול-רישום — היעדר "הוראת קבע" אינו מוכיח שרוצים לבטל צהרון. תוקן. וואטסאפ בלבד; לא נפרס.

## התיקון — סיווג אזכור ביטול לפי *מטרה* ([flows.ts](../src/lib/bot/flows.ts))
| קלט | תוצאה |
|---|---|
| **ביטול רישום מפורש** (רישום/צהרון/קייטנה, חיובי) — "לבטל את הרישום" | מסלול הביטול (`cancel_child`) |
| **עמום** (בלי מטרה ברורה) — "רוצה לבטל" / "לבטל" | **בירור** (`payset_cancel_clarify`): "מה תרצו לבטל - את הרישום לצהרון או הסדרת התשלום?" |
| **ביטול אמצעי תשלום** — "לבטל את הוראת הקבע" | תפריט חלופות (מענה מתאים, **לא** ה-re-ask המעגלי) |
| **שאלה** — "אפשר לבטל…?" | LLM |
| **שלילה** — "לא רוצה לבטל" | החזרה להצעה |

מנגנון: `regTarget` (רישום/צהרון/קייטנה) חיובי → ביטול; `payTarget` (הוראת קבע/הו"ק/התשלום) → חלופות; בלי מטרה → בירור. **מעבר לביטול-רישום רק במטרת-רישום מפורשת.**

## הודעה חדשה ניתנת לעריכה
`payset_cancel_clarify` (קטגוריית "הסדרת תשלום") — נכנסה ל-`BOT_MESSAGE_REGISTRY`, ניתנת לעריכה בדשבורד.

## קורפוס (232 בדיקות)
- **cancel-redirect** (מטרת-רישום): "לבטל את הרישום"/"לבטל את הצהרון" → `cancel_child`.
- **cancel-ambiguous (חדש):** "רוצה לבטל"/"לבטל"/"אני רוצה לבטל" → בירור (`payment_setup_offer` + "מה תרצו לבטל"). **תוקנה הציפייה שקיבעה ניתוב שגוי.**
- **cancel-guard:** שאלה/שלילה/אמצעי-תשלום → *לא* `cancel_child`.

## אימות
- `tsc` נקי · `replay` **232/232** · `infra` **46/46** · **fail-before מוכח** (3 טענות נכשלות בניתוב הרחב).

## נותר בחלק ב' (חסום — טרם ענו הלקוח)
1.3 מדיניות ביטול · גבעתיים · §3 קובץ רפואי · §4 שנת לידה.
