# הנדאוף לסקירת astra - חלק ב' #7 מסלול תשלום, סבב 12: בירור "מה לבטל" ללא היקש (22.9.2026)

**בסיס:** `6c34b9a` (סבב 11). **ברנץ':** `claude/kids-fun-astra-review-0ef589`. **לא נפרס, לא מוזג.** WhatsApp בלבד.

## מה astra מצא (סבב 11) ומה תוקן
- **P2 - נסיגה שלי:** `(reg && pay)` הסיק "שניהם" מעצם הופעת שתי מילות-מטרה. "את הוראת הקבע לצהרון" / "הוראת הקבע של הצהרון" / "התשלום לצהרון" → ביטול רישום (בסבב 10 נשארו בבירור).
  **תיקון:** אין היקש. שתי מטרות חיוביות מובילות לביטול **רק עם חיבור מפורש** (`וגם` / `גם ... גם`). שילוב בלי חיבור → לא מוכרע → בירור שוב, כמו בשלב ההצעה.
- **שימושיות - "לא שתי האפשרויות" → חלופות:** הביטוי המשותף הוחרג מבקשת-חלופה רק כשהוא חיובי. **תיקון:** מוחרג בכל הופעה (`both === 'none'` בלבד מאפשר `wantsAlt`). "משהו אחר, לא הוראת קבע" עדיין → חלופות.

## סריקה יזומה נוספת על שלב הבירור (לפני הסבב הזה)
הרצתי 33 ניסוחים נוספים על השלב ("רק את הרישום", "הרישום בלבד", "הרישום או התשלום?", "בעצם כלום, תמשיכו", "את הרישום? לא, את התשלום"...). **פער אחד** שאסטרה טרם ניסה: **"לא בטוח, אולי את הרישום" → ביטול רישום.** היסוס ("אולי", "אבל", "רגע", "לא בטוח", "לחשוב", "תלוי", "כנראה", "אני חושב/ת") הוא אותה מחלקה שכבר ננעלה בשלב ההצעה (`CONFIRM_QUALIFIER_RE`/`DEFER_RE`) וחסרה כאן. **תיקון:** `HESITATION_RE` בשלב הבירור → בירור שוב. בכוונה בלי "רק": "רק את הרישום" היא בחירה ברורה (נעול בקורפוס).

## ניתוב שלב הבירור אחרי הסבב (סדר מלא)
| # | תנאי | ניתוב |
|---|---|---|
| 1 | שאלה / בקשת הסבר | LLM, נשאר בבירור |
| 2 | בקשת חלופה מפורשת, בלי ביטוי-משותף כלל ובלי רישום חיובי | חלופות |
| 3 | שלילה של כל מטרה (רישום / תשלום / שניהם) **או היסוס** | בירור שוב |
| 4 | "שניהם" מפורש וחיובי, **או** רישום+תשלום חיוביים **עם** `וגם`/`גם...גם` | ביטול רישום (אישור נפרד שם) |
| 5 | רישום חיובי בלבד | ביטול רישום |
| 6 | תשלום חיובי בלבד | חלופות |
| 7 | אחרת (כולל רישום+תשלום בלי חיבור, "1", "כלום") | בירור שוב |

## נקודת הכרעה
"את הרישום ואת הוראת הקבע" (חיבור ב-"ו" בלבד, בלי "גם") → **בירור**, לא ביטול. בחרתי להכיר רק ב-`וגם`/`גם...גם` כחיבור מפורש, כי "ו" לבדה מופיעה גם בתיאורים ("הוראת הקבע והחיוב"). אם astra מעדיף להכיר גם ב-"ואת X", זו הרחבה של ביטוי אחד ברגקס, לא שינוי מנגנון.

## קבצים
- `src/lib/bot/flows.ts` - שלב `payment_setup_cancel_choice`: `explicitPair`, `wantsAlt` עם `both === 'none'`, `HESITATION_RE`.
- `scripts/replay-bot.ts` - +12 מקרים (cancel-choice-guard 8: 3 של astra + "ואת" + "לא שתי האפשרויות" + 3 היסוס; cancel-choice 4: "רק"/"בלבד"/"גם...גם").

## שער אימות
`npx tsc --noEmit` נקי · replay **334/334** (היה 322) · `infra-tests.cjs` **46/46** · **fail-before: 7 טענות נופלות על `6c34b9a`** (3 הדוגמאות של astra, "ואת", 3 היסוס), עוברות על התיקון. בדיקות הקבלה של astra: "שניהם", "לא, שניהם", "גם את הרישום וגם את הוראת הקבע" → ביטול; השלילות מסבב 10 → בירור; "גם את התשלום" → חלופות. ליבת dedup/CAS/rev, שלב ההצעה ו-R6 לא נגעו.
