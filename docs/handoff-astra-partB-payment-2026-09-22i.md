# הנדהוף לסקירה חוזרת — חלק ב' #7, שלב בירור-ביטול ייעודי, commit `54b0a80`

astra: הבירור נוסף אך הרצף לא נסגר. 2 פערים — תוקנו. וואטסאפ בלבד; לא נפרס.

## פער 1 — מענה הבירור לא נשמר ✅
"רוצה לבטל" → "מה תרצו לבטל?" → "את הרישום" **הפעיל רישום חדש** (ה-intent classifier תפס "רישום" ככוונת רישום), במקום להיקרא כמענה לבירור.

**תיקון:** **שלב בירור ייעודי `payment_setup_cancel_choice`.** הבירור מוביל אליו, והתשובה נקראת *שם*:
- "את הרישום" / "הרישום" / "צהרון" → מסלול הביטול.
- "אפשרות אחרת" / "הוראת קבע" / "תשלום" → חלופות.
- לא ברור → בירור שוב.

handler: השלב מחזיק `רישום_צהרון`/`רישום_קייטנה`/`ביטול` ב-`STEP_OWN_INTENTS` — כך התשובה לא נחטפת לכוונה חדשה. (+`CHOICE_STEPS`.)

## פער 2 — שתי מטרות יחד ✅
"לבטל את הוראת הקבע **לצהרון**" הפעיל ביטול-רישום כי "צהרון" גבר על "הוראת קבע".

**תיקון — ניתוב לפי מטרה בלעדית:**
| מטרה | תוצאה |
|---|---|
| רישום *בלבד* (בלי אזכור אמצעי-תשלום) | מסלול הביטול |
| אמצעי-תשלום *בלבד* | חלופות |
| **שתי המטרות יחד** / עמום | **בירור** (שלב ייעודי) |

## קורפוס (238 בדיקות)
- cancel-redirect (רישום בלבד): "לבטל את הרישום"/"לבטל את הצהרון" → `cancel_child`.
- cancel-ambiguous: "רוצה לבטל"/"לבטל"/**"לבטל את הו"ק לצהרון"** → `payment_setup_cancel_choice` (בירור).
- **cancel-choice (חדש):** מהבירור — "את הרישום"/"צהרון" → `cancel_child`; "אפשרות אחרת"/"הוראת קבע" → `payment_setup_method`.
- cancel-guard: שאלה/שלילה/אמצעי-תשלום-בלבד → *לא* `cancel_child`.

## אימות
- `tsc` נקי · `replay` **238/238** · `infra` **46/46** · **fail-before מוכח** (5 טענות).

## נותר בחלק ב' (חסום — טרם ענו הלקוח)
1.3 מדיניות ביטול · גבעתיים · §3 קובץ רפואי · §4 שנת לידה.
