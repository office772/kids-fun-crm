# הנדאוף לסקירת astra - חלק ב' #7 מסלול תשלום, סבב 14: "שניהם" רק ממשפטים שלמים בהתאמה מלאה (22.9.2026)

**בסיס:** `860c88e` (סבב 13). **ברנץ':** `claude/kids-fun-astra-review-0ef589`. **לא נפרס, לא מוזג.** WhatsApp בלבד.

## מה astra מצא (סבב 13) ומה תוקן
**P2:** `CANCEL_PAIR_RE` התאים לתת-מחרוזת עם wildcard, ולכן "את התשלום לצהרון וגם את התשלום לקייטנה" (שני תשלומים) → ביטול רישום. אותו שורש כמו בסבבים 11-12.

**תיקון לפי ההמלצה, ללא חריגים וללא הרחבת regex:**
- `CANCEL_PAIR_RE` ו-`explicitPair` **הוסרו**. אין עוד שום היקש של "שניהם" ממילים מפוזרות.
- נשארת רשימה סגורה `CANCEL_BOTH_SENTENCES` (14 משפטים) בהתאמה **מלאה** להודעה המנורמלת (`confirmCore`): "גם את הרישום וגם את הוראת הקבע", "את הרישום וגם את התשלום", "הרישום והתשלום" וכדומה. מילה אחת נוספת בהודעה = אין התאמה.
- ניתוב: "שניהם"/"הכל" מפורש (`CANCEL_BOTH_RE`, ללא שינוי) **או** משפט שלם מהרשימה → ביטול רישום (אישור נפרד שם). כל שילוב אחר של רישום+תשלום → בירור שוב.

## נקודת הכרעה
הכנסתי לרשימת המשפטים השלמים גם צורות עם "ואת"/"ו" ("את הרישום ואת הוראת הקבע", "הרישום והתשלום"). בסבב 11 הוסכם שבירור ל-"ואת" מקובל; כאן זו התאמה מלאה של ההודעה כולה, בלי wildcard, ולכן אין סיכון של תיאור-תשלום שנתפס. אם astra מעדיף לשמור "ואת" בבירור, מוחקים 4 משפטים מהרשימה, ומחזירים מקרה אחד בקורפוס לבירור.

## קבלה של astra
"את התשלום לצהרון וגם את התשלום לקייטנה" → בירור ✔ · הדוגמאות מסבב 12 → בירור ✔ · "שניהם"/"לא, שניהם" → ביטול ✔ · שלילות/היסוסים/"לא שתי האפשרויות" → בירור ✔ · רישום בלבד → ביטול, תשלום בלבד → חלופות ✔. בדיקות של שאלה/שלילה/היסוס/ניתוב חד-משמעי לא שונו.

## קבצים
- `src/lib/bot/flows.ts` - `CANCEL_BOTH_SENTENCES` במקום `CANCEL_PAIR_RE`; `bothSentence` במקום `explicitPair`.
- `scripts/replay-bot.ts` - +5 (guard: הדוגמה של astra + 3 שילובים חופשיים → בירור; cancel-choice: 2 משפטים שלמים; "את הרישום ואת הוראת הקבע" הועבר מ-guard למשפט שלם).

## שער אימות
`npx tsc --noEmit` נקי · replay **343/343** (היה 338) · `infra-tests.cjs` **46/46** · **fail-before: 6 טענות נופלות על `860c88e`** (4 שילובים חופשיים שהיו ביטול, 2 משפטים שלמים שהיו בירור), עוברות על התיקון. ליבת dedup/CAS/rev, שלב ההצעה ו-R6 לא נגעו.
