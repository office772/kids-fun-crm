# הנדאוף — עריכה בדשבורד: 4 הפאזות בוצעו · 18.9.2026

> ממשיך את `docs/handoff-2026-09-17-part2-build-plan.md`. כל 4 הפאזות שתוכננו שם — בוצעו ואומתו.
> **ברנץ' `claude/kids-fun-dashboard-editing-9e538d` — לא מוזג, לא נדחף, לא נפרס.** HEAD `03d7105` מעל `65bb2e8` (prod 2.1.2).

## 0. סטטוס
- **tsc נקי · replay-bot 98/98** (`ANTHROPIC_API_KEY=dummy npx tsx scripts/replay-bot.ts`).
- **פרודקשן ללא שינוי** — כל אימות חי שוחזר: `settings`=6 מפתחות מקוריים, `schools.monthly_price` כולם null, `bot_messages` ריקה. עיקרון־על: בלי הגדרה/DB → הבוט מתנהג בדיוק כמו לפני.
- **טרם נפרס.** לפי הכלל — **לא לפרוס באמצע סבב הבדיקות של הלקוח.**
- worktree חדש: יש בו `.env.local` (הועתק מה-main checkout) ו-node_modules מלא (הורץ `npm install` — היה חסר `@vercel/functions`).

## 1. מה נבנה (6 קומיטים)
| קומיט | פאזה |
|---|---|
| `d84d5cf` | 1 — קול הבוט + `/api/admin/settings` |
| `b82c457` | 2 — הגדרות מערכת עריכות + הבוט קורא מ-DB |
| `6f28f00` | 3 — CRUD מסגרות + מחיר לפי מסגרת |
| `526fbe3` | מדריך משתמש (HelpGuide) |
| `03d7105` | 4 — עריכת טקסט הודעות (סט התחלה) |

**קבצי מפתח חדשים:** `src/lib/bot/{settings-db,bot-voice,schools-db,bot-messages-db}.ts` · `src/app/api/admin/{settings,schools,bot-messages}/route.ts` · `src/components/dashboard/{BotVoiceSettings,BotContentManager(שוכתב),SystemSettings(שוכתב)}.tsx` · `src/components/admin/FrameworksManager.tsx`.

**ארכיטקטורה חוצת-פאזות:** cache פר-בקשה (`prime*Cache` ב-webhook+simulate לפני `processMessage`) לקריאה סינכרונית מתוך flows, תמיד עם fallback לקשיח. מיגרציות ב-Supabase: `add_schools_monthly_price`, `add_bot_messages_table`.

## 2. מה הלקוח יכול לערוך עכשיו (אומת חי)
- **שעות/ימי פעילות + מחיר ברירת מחדל** — דשבורד → תוכן הבוט → הגדרות מערכת → כללי. `isBusinessHours`/מחיר קוראים מ-DB.
- **קול הבוט** — אותה לשונית → קול הבוט. מוזרק ל-system prompt של ה-LLM. **ריק = ללא שינוי** (לא לגעת בטון עד החלטת פרסונה).
- **מסגרות** — פאנל ניהול (`/admin`) → מסגרות: הוספה, מחיר, קיבולת, כיבוי. `resolveMonthlyFee` מעדיף `schools.monthly_price`.
- **טקסט הודעות** — דשבורד → תוכן הבוט → תכני הודעות. **סט התחלה של 5 הודעות** (תפריט, "לא הבנתי", 2× הסלמה, אישור אזור). משתנים `{…}` נעולים (ולידציה בצד לקוח+שרת).
- **שאלות ותשובות / קיבולת אזורים / צוותים / קבצים** — כבר עבדו לפני.

## 3. מה נשאר
1. **פריסה** (כשבטוח, לא באמצע בדיקות): `git merge --ff` ל-main → `vercel login` כ-office772 → `vercel --prod` → בדיקת עשן. המיגרציות כבר הוחלו ב-Supabase (חיות).
2. **הרחבת פאזה 4** — הוספת הודעות נוספות לרג'יסטרי `BOT_MESSAGE_REGISTRY` + חיווט ה-call-site התואם ב-`flows.ts` (יש 153 `text:`). כל תוספת: default = הטקסט הקשיח הקיים בדיוק, וציון המשתנים המותרים.
3. **ניקוי יתומים:** `src/components/admin/SchoolCapacitySettings.tsx` ו-`/api/admin/school-capacity/route.ts` כבר לא בשימוש (הוחלפו ב-FrameworksManager).
4. אופציונלי: לחווט **טוגלים אמיתיים** (bot_active/registration_open) — כרגע לא נקראים בקוד, לכן לא נחשפו במסך.

## 4. דגשים
- **`botText` ולא `msg`** — הפרמטר `msg` נפוץ מאוד ב-flows.ts; שם אחר מונע shadowing.
- כל מסך עריכה שומר **רק שדות שהבוט באמת קורא** — לא לחזור לטעות של "מסך דמו ששומר מדומה" (65bb2e8).
- לפני deploy: **תמיד** tsc + replay ירוקים.
