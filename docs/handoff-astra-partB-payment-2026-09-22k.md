# הנדאוף לסקירת astra - חלק ב' #7 מסלול תשלום, סבב 11: שלב הבירור "מה לבטל" (22.9.2026)

**בסיס:** `5641a27` (סבב 10). **ברנץ':** `claude/kids-fun-astra-review-0ef589`. **לא נפרס, לא מוזג.** WhatsApp בלבד.

## מה astra מצא (סבב 10) ומה תוקן
שני P2 באותו ענף חדש ("שניהם") בשלב `payment_setup_cancel_choice`: (1) "לא שניהם"/"לא הכל" → ביטול רישום, כי השלילה נבדקה רק ביחס למילת-רישום; (2) "גם את התשלום"/"גם את הוראת הקבע" → ביטול רישום, כי `גם את` נחשב "שניהם" והענף רץ לפני בדיקת התשלום.

**המחלקה, לא הנקודות:** בשלב הבירור יש שלושה ביטויי-מטרה (רישום / תשלום / "שניהם") וכל אחד יכול להופיע חיובי או שלול. קודם רק הרישום נקרא עם השלילה שלו. עכשיו **כל ביטוי-מטרה נקרא עם השלילה שבפסוקית שלו** (`target()` → `yes | negated | none`, על הגולמי דרך `negatedInClause`), והניתוב מוכרע לפי שלושת המצבים יחד:

| סדר | תנאי | ניתוב | דוגמאות |
|---|---|---|---|
| 1 | שאלה / בקשת הסבר | LLM (נשאר בבירור) | "מה יקרה לרישום?" |
| 2 | בקשת חלופה מפורשת (`אפשרות אחרת`/`משהו אחר`/`חלופ`/`אפשרויות`/`תפריט`) בלי רישום חיובי | חלופות | "משהו אחר, לא הוראת קבע", "אפשרות אחרת בבקשה" |
| 3 | **שלילה של כל מטרה** (רישום / תשלום / "שניהם") | בירור שוב | "לא שניהם", "לא הכל", "לא גם וגם", "לא את הרישום", "לא את התשלום" |
| 4 | "שניהם" מפורש וחיובי **או** שתי מטרות חיוביות | ביטול רישום (כולל הו"ק, עם אישור נפרד) | "שניהם", "לא, שניהם", "את שתי האפשרויות", "גם את הרישום וגם את הוראת הקבע" |
| 5 | רישום בלבד | ביטול רישום | "את הרישום", "גם את הצהרון" |
| 6 | תשלום בלבד | חלופות | "את התשלום", "גם את התשלום", "גם את הוראת הקבע" |
| 7 | אחרת | בירור שוב | "לא בטוח" |

- `CANCEL_BOTH_RE`: הוסר `גם את`; נשאר `שניהם|שתיהן|הכל|גם וגם|שתי האפשרויות|שני הדברים`. "שתי האפשרויות" אינה נספרת כבקשת חלופה (מונע התנגשות עם `אפשרויות`).
- ההבחנה המכוונת נשמרת: "לא, שניהם" (פסיק = פסוקית נפרדת) → שניהם; "לא שניהם" → בירור.
- מטרת התשלום נקראת מהגולמי (`הוראת קבע` / `הו"ק` על כל צורות הגרש / `תשלום` / `אמצעי`).

## נקודת הכרעה
- **"לא את התשלום"** (שלילת תשלום בלבד): בירור שוב, לא היקש "אז את הרישום". בחרתי לא להסיק ביטול רישום משלילה. אם astra מעדיף היקש, זה שינוי של שורה, אבל לדעתי הצד הבטוח נכון.
- **"וואלה למה לא"** (הערת שימושיות מסבב 10): נשאר חלופות. לא שיניתי, כי הפרדת "למה לא" רטורי משלילה אמיתית דורשת היוריסטיקה חדשה, בניגוד לעיקרון. אם רוצים, ההצעה היא ביטוי שלם `וואלה למה לא` ברשימת "הבהרה" (unclear), לא אישור.
- ניסוח הקורפוס לפי הערת astra: **קורפוס מגוון עם רגרסיות מוכרות**, לא כיסוי של כל מרחב השפה.

## קבצים
- `src/lib/bot/flows.ts` - שלב `payment_setup_cancel_choice` (ניתוב לפי שלושת ביטויי-המטרה), `CANCEL_BOTH_RE`.
- `scripts/replay-bot.ts` - +9 מקרים (cancel-choice 5, cancel-choice-guard 4).

## שער אימות
`npx tsc --noEmit` נקי · replay **322/322** (היה 313) · `infra-tests.cjs` **46/46** · **fail-before: 6 טענות נופלות על `5641a27`** ("גם את התשלום", "גם את הוראת הקבע", "את שתי האפשרויות", "לא שניהם", "לא הכל", "לא גם וגם"), עוברות על התיקון. ליבת dedup/CAS/rev, שלב ההצעה, ו-R6 לא נגעו.
