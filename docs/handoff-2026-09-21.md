# הנדאוף לסשן הבא - Kids & Fun bot (21.9.2026 ערב)

## איפה אנחנו
עינת הריצה 2 סקירות עצמאיות עם **astra (OpenAI)** לפני אישור הבוט = **14 ממצאים** (8 שיחה + 6 תשתית).
כולם אומתו מול הקוד וה-PDF של הלקוח. **11 תוקנו + §5**, כל תיקון עם בדיקת רגרסיה. **לא נפרס.**

- **ברנץ':** `claude/kids-fun-astra-review-0ef589` (worktree `kids-fun-dashboard-editing-9e538d`)
- **commit:** `667cb84` (יחיד מעל main), נדחף origin (office772/kids-fun-crm) + backup
- **פרודקשן:** `main=c9b5cf1`, webhook `2.2.0` - **לא נגענו** (הלקוח בודק את זה)
- **בדיקות:** `tsc` נקי · `replay-bot.ts` 115/115 · `scripts/infra-tests.cjs` (חדש, harness מבודד) 16/16

## הצעד הראשון בסשן הבא (לבדוק לפני שממשיכים)
1. **האם astra סיים לסקור את החלק הראשון?** ההנדאוף לסקירתו: `docs/handoff-astra-review-2026-09-21.md`.
   אם יש הערות - לקרוא, לתקן, ולאמת מחדש (tsc + replay + infra). נקודות שביקשנו ממנו לאתגר:
   טוקן `last_message_at` מול version-column (#12) · dedup-gate אחרי loadSession (#13) · אידמפוטנטיות
   מלאה שלא מומשה · #9 sandbox חלקי · נוסח #6.
2. **האם הלקוח החזיר תשובה על מדיניות הביטול?** (השאלה נשלחה 21.9 - 4 שאלות: עד/אחרי ה-15,
   זיכוי, מתי נעצרת הו"ק, דוגמה ל-21 בחודש). זה **חוסם** את 1.3.

## מה תוקן (מפורט: ראה הנדאוף astra + commit 667cb84)
- **בטיחות ביטול:** #2 אישור חד-משמעי בקוד (whitelist פר-מסלול) · #3 `performCancellation` בודק כשל update
- **תשתית:** #9 סימולטור לא נוגע בנתונים (`session.simulated`) · #10 איסוף מאמת הורה-ילד · #11 notify דרך
  `uchat.sendText` המשותף · #12 שמירה מותנית-גרסה `last_message_at` · #13 dedup אטומי `id_message` · #14 כשל שמירה→retry
- **ניתוב:** #4 נציגה גוברת · #8 נרמול ספרות · #5 "גן"≠ת"א · #6 סירוב הו"ק · §5 הסרת תשלום חלופי לפני טופס

## חסום בהחלטות - השלב הבא (לפי סדר)
1. **1.3 מדיניות ביטול** - תלוי תשובת הלקוח. יש 3 גרסאות סותרות (אפיון brief_ganspan_v2 / קוד / PDF §12).
   כשתחזור התשובה: לבנות helper יחיד `cancellationPolicy(dayOfMonth)` → `{endDate, policyText}`, ליישר
   **4 קבצים** (`bot-messages-db.ts`, `flows.ts`, `llm-fallback.ts`, `content-map.ts`), להזריק תאריך קונקרטי,
   ולממש את הצד הכספי (מתי נעצרת הו"ק, סטטוס "פעיל עד תאריך" בלי להוציא מנוכחות מוקדם). בדיקות: 15/16, מעברי חודש.
2. **#7 §10-11 מסלול תשלום** - הו"ק כאופציה ראשית בלי תפריט, חלופות רק אחרי סירוב. (מתחבר ל-#6.)
3. **גבעתיים (#5-נתונים)** - אזור פעיל ב-PDF עמ' 6, חסר מ-`AREAS`. צריך לינק טופס מהלקוח, או לנתב לקורלי.
4. **§4 שנת לידה** - החלטה: אזהרה רכה (שליחה + דגל צוות) או חסימה. `register/page.tsx:149`.
5. **§3 קובץ רפואי** - פיצ'ר (Supabase Storage). `register/page.tsx:381`.
6. **#9 sandbox מלא + Auth לדשבורד** - התיקון חוסם ביטול הרסני; sandbox מלא לכל הכתיבות + auth תלוי החלטה.
7. **פרסונה (ג'וני)** - שם/מגדר, חסום עד החלטת לקוח.

## פריסה (כשמאשרים - לא באמצע סבב בדיקות!)
`vercel login` כ-**office772** (device-code, עינת) → `git merge --ff-only` ל-main (הברנץ' commit יחיד מעל
c9b5cf1) → `vercel --prod` מ-main checkout → בדיקת עשן (webhook GET version · route 200 · תפריט).
**הלקוח מתנסה מחרתיים** אם התשובות (astra + מדיניות ביטול) חוזרות בזמן; אחרת לפרוס הכל ולהשאיר את נוסח הביטול לסוף.

## אימות
```bash
npx tsc --noEmit
ANTHROPIC_API_KEY=dummy npx tsx scripts/replay-bot.ts   # 115/115
node scripts/infra-tests.cjs                            # 16/16
```

## מקורות
- סקירות astra: `~/.codex/visualizations/2026/09/21/01a0c4fc-.../kids-fun-review.md` + `kids-fun-review-extended.md`
- הערות הלקוח: `~/Downloads/הערות לבדיקות בוט עינת גן-אל.pdf` (6 עמ')
- אפיון: `~/Documents/claude projects/kids & fun/brief_ganspan_v2.docx`
- הערת מוח: `[[Kids Fun CRM Bot]]` · זיכרון: `kids-fun-project` · יומן: `40-יומן/2026-09-21.md`
- שאלת מדיניות הביטול ללקוח: בצ'אט של סשן 21.9 (לא נשמרה כקובץ, בבקשת עינת)
