# הנדהוף להמשך — Kids & Fun בוט, סקירת astra (22.9.2026)

**מצב:** על הברנץ' `claude/kids-fun-astra-review-0ef589` (worktree `kids-fun-dashboard-editing-9e538d`). **לא נפרס. לא מוזג ל-main.** HEAD = `1b12355`.
נדחף ל-origin (`office772/kids-fun-crm`) + backup (`ganeleinat-ai1975/kids-fun-app-backup`).

## התהליך (חובה להמשיך כך)
עינת מריצה סקירות עצמאיות עם מודל **astra** (OpenAI). כל סבב: astra מוצא פערים → אני מתקן + מוסיף בדיקת רגרסיה (fail-before/pass-after) → commit+push (origin+backup) → כותב הנדהוף לסקירה חוזרת → מעדכן מוח. **וואטסאפ בלבד דרך uChat; הסימולטור מחוץ להיקף.**
- **כללי עבודה:** עברית RTL · רק מקף רגיל (לא —/–) · לא לומר "בוט" ללקוח · לפני push לבדוק סודות (`git diff --cached`) · לא לפרוס באמצע בדיקות · בסוף כל סבב לגבות origin+backup+מוח.
- **שער אימות בכל שינוי:** `npx tsc --noEmit` נקי · `ANTHROPIC_API_KEY=dummy npx tsx scripts/replay-bot.ts` · `node scripts/infra-tests.cjs`. **מצב נוכחי: replay 243/243 · infra 46/46 · tsc נקי.**

## חלק א' (בטיחות/מקביליות) — ✅ סגור ואושר ע"י astra
פאזות 0-3: בידוד בדיקות · בטיחות ביטול (CAS) · תשתית/מקביליות (rev, claim/token, dedup אטומי, סגירת סשן עמידה, אימות שמירת פנייה) · ניתוב. **המיגרציה `supabase/migrations/add_bot_sessions_rev.sql` חובה לפני deploy.**

## חלק ב' #7 — מסלול תשלום (הוראת קבע ראשית) — ⏳ ממתין לסקירת astra של `1b12355`
**מה מומש:** במקום תפריט 6 שיטות מיד → מציעים הוראת קבע ישירות; חלופות רק אחרי סירוב. ההודעות ניתנות לעריכה בדשבורד (`BOT_MESSAGE_REGISTRY`, משתנים נעולים).

**קבצים מרכזיים:**
- `src/lib/bot/flows.ts` — `handlePaymentSetupFlow`: שלב `payment_setup_offer` (הצעת הו"ק) + `payment_setup_cancel_choice` (בירור "מה לבטל"). `offerVerdict()` = מסווג תגובה (accept/refuse/question/unclear).
- `src/lib/bot/handler.ts` — `CHOICE_STEPS` + `STEP_OWN_INTENTS` כוללים את שני השלבים החדשים.
- `src/lib/bot/bot-messages-db.ts` — הודעות: `payset_offer_standing`(+`_no_price`), `payset_offer_reask`, `payset_cancel_clarify`, `payset_intro_menu` (חלופות).
- `scripts/replay-bot.ts` — **קורפוס ממצה `OFFER`** (accept/question/refuse/unclear) + קטגוריות cancel-redirect/ambiguous/choice/guard.

**עקרונות שננעלו (אחרי 6 סבבי astra על אותו מסווג — ראו לקח):**
1. **אישור = התאמה מלאה לביטוי שלם** (`CONFIRM_STANDING`), לא regex תחילת-משפט/אורך/token. מילה בודדת ("אני"/"הוראת") אינה אישור. ברירת מחדל בטוחה = הבהרה.
2. **סדר קדימות ב-offerVerdict:** שאלה/הסבר → דחייה → סירוב → אישור → עמום.
3. **מחיר מוצג רק אם מאומת** (`monthly_fee` נקבע). אחרת `payset_offer_standing_no_price`.
4. **ניתוב ביטול:** מטרת-רישום *בלבד* → מסלול ביטול; אמצעי-תשלום *בלבד* → חלופות; שתי מטרות/עמום → שלב בירור `payment_setup_cancel_choice`. בשלב הבירור: **קודם הגנות שאלה/שלילה, ואז בחירת מטרה** (התיקון האחרון, `1b12355`).

**היסטוריית הסבבים של #7 (הנדהופים ב-`docs/handoff-astra-partB-payment-*.md`):** מסלול ראשוני → מחיר מאומת+אישורים טבעיים → קדימות שאלות → ביטוי שלם → מסווג עמיד+קורפוס → ביטויים שלמים+סירוב/ביטול → ניתוב ביטול זהיר → ביטול עמום→בירור → שלב בירור ייעודי → הגנות בבירור (`...i.md` = האחרון).

## ⚠️ לקח קריטי (בזיכרון `audit_whole_bug_class`)
astra מצא **8+ סבבים** את אותו מסווג אישור/ביטול כי תיקנתי דוגמה-דוגמה. **למסווג שפה: אוצר/רשימה סגורה של ביטויים שלמים + קורפוס ממצה מקוטלג + ברירת מחדל בטוחה (הבהרה). כל תיקון = ביטוי שלם לרשימה + מקרה לקורפוס, לא regex/heuristic חדש.** לפני שמכריזים "סגור" — למפות את *כל* מרחב הקלט בקורפוס.

## הצעד הבא בסשן החדש
1. להמתין לסקירת astra של `1b12355` (מסלול התשלום #7). אם מוצא פער — להוסיף לקורפוס `OFFER`/cancel-* ולתקן לפי העקרונות למעלה, **לא נקודה-נקודה**.
2. כשמסלול התשלום נסגר → לשאר חלק ב', **חסום בהחלטות לקוח שטרם ענו:** 1.3 מדיניות ביטול כספי · גבעתיים (אזור) · §3 העלאת קובץ רפואי · §4 שנת לידה. (הפרסונה "ג'וני" אושרה; עריכת הטונים בידי הלקוח דרך עורך ההודעות.)
3. אחרי אישור astra מלא → החלת המיגרציה + אינטגרציה מול DB בדיקות אמיתי + בדיקות WhatsApp מבוקרות מול הצוות → ואז deploy.

## הודעת פתיחה מוצעת לסשן הבא
"ממשיכים סקירת astra ל-Kids & Fun (ברנץ' `claude/kids-fun-astra-review-0ef589`, HEAD `1b12355`, לא נפרס). חלק א' סגור; #7 מסלול תשלום ממתין לסקירה. כשמגיע דוח astra — לתקן לפי העקרונות שבהנדהוף (רשימת ביטויים שלמים + קורפוס, לא נקודה-נקודה). שער אימות: tsc+replay+infra."
